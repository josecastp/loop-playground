/**
 * El bucle infinito
 *
 * El siguiente bucle 'while', no acaba nunca. ¿Por qué?
 * 
 * Arreglalo para que la palabra "Ejecutar!" se muestre 10 veces por el terminal
 */
//Por que i es menor que 10 y mientras no se incremente el valor de 1 siempre se ejecutará
let n=10;
let i=0;

while(i<n) {
    console.log("Ejecutar!")
    i++;
}

